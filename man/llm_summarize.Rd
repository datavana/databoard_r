% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{llm_summarize}
\alias{llm_summarize}
\title{Automated content summarisation with an LLM}
\usage{
llm_summarize(data, col, rules, options = list(), wait = 0)
}
\arguments{
\item{data}{A data frame containing the texts to be summarised, or a data
frame previously returned by \code{llm_summarize()} whose pending results
should be fetched.}

\item{col}{A column in \code{data} holding the input text. Tidy-evaluation is
supported (pass the bare column name).}

\item{rules}{Optional. Provide rules to produce multiple summaries.
A data frame with the columns \code{category}, \code{description}, and \code{example}.
One output is created for each category. The description should prompt
what to summarize. Optionally, add an example of the expected output.}

\item{options}{A named list of additional options passed to the Databoard
server.}

\item{wait}{Integer. Seconds to wait server-side per case for the task to
complete before returning.
\itemize{
\item \code{0} (default): submit all tasks and return immediately with state
\code{PENDING}. Fetch results later by calling \code{llm_summarize(data)} again.
\item \verb{> 0}: wait up to that many seconds per case for the result.
}}
}
\value{
The input data frame with the columns \code{.task_id} and \code{.task_state}
added. When results are available, they are unnested into additional
result columns (e.g. \code{llm_result}).
}
\description{
Submits each row of \code{data} to the Databoard \code{summarize} task, which asks
an LLM to summarise the text in \code{col}. Optionally, a \code{rules} data frame
can be provided to produce structured, per-category summaries.
The databoard server pre-processes the rules to embed them in the prompt,
and post-processes the LLM answer to split it into columns for a data frame.
}
\details{
The function has two modes of operation, dispatched automatically:
\itemize{
\item \strong{Submit} — if \code{data} does \emph{not} yet contain a \code{.task_id} column, the
texts in \code{col} are submitted as new summarisation tasks (via
\code{\link[=da_submit]{da_submit()}}).
\item \strong{Fetch} — if \code{data} already contains a \code{.task_id} column (i.e. it was
previously returned by \code{llm_summarize()}), the function fetches results
for any tasks that are still pending (via \code{\link[=da_fetch]{da_fetch()}}). In this case,
all other parameters are ignored.
}

Typical usage is to call \code{llm_summarize()} once to submit, and then call
it repeatedly on the returned data frame until all results are in (see
\code{\link[=da_finished]{da_finished()}}).

To customize the prompts, provide them in the options:

\if{html}{\out{<div class="sourceCode">}}\preformatted{options = list(
  prompts = list(
    system = "YOURSYSTEMPROMPT",
    user = "YOURUSERPROMPT"
  )
)
}\if{html}{\out{</div>}}

If the prompts are given as character vectors, their elements are collapsed
using a line break (as in \code{\link[=llm_prompt]{llm_prompt()}}).

If present in the prompts, the placeholder \code{{{text}}} is replaced by the value of the current case.
The placeholder \code{{{rules}}} is replaced by a rule book generated from the rules data frame.

See the databoard documentation for further options.
}
\examples{
\dontrun{
# Log in to the Databoard service (requires valid credentials)
da_login()

# Submit summarisation tasks
data <- llm_summarize(songs, text)

# Fetch results: call the same function again with the returned
# data frame as the only argument. Repeat until all results are in.
data <- llm_summarize(data)

# Inspect the summaries
head(data$llm_result)
}

}
\seealso{
\code{\link[=llm_prompt]{llm_prompt()}}, \code{\link[=llm_code]{llm_code()}}, \code{\link[=da_submit]{da_submit()}}, \code{\link[=da_fetch]{da_fetch()}}]
}
