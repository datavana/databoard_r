% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tasks_run_get}
\alias{tasks_run_get}
\title{Retrieve a single task result from the Databoard API (low level)}
\usage{
tasks_run_get(task_id, wait = 0)
}
\arguments{
\item{task_id}{Character. The identifier of a previously submitted task.}

\item{wait}{Integer. Seconds to wait server-side for the task to complete
before returning. Defaults to \code{0}.}
}
\value{
The server response.
}
\description{
Sends one \code{GET /tasks/run/{task_id}} request to the Databoard API. This is
the underlying request helper used by \code{\link[=da_fetch]{da_fetch()}}; end users typically
don't need to call it directly.
}
\details{
Requires prior authentication via \code{\link[=da_login]{da_login()}}; the access token
is read from the \code{DATABOARD_ACCESSTOKEN} environment variable. Stops with
an informative error if the token is missing or the HTTP status is not
2xx.
}
\keyword{internal}
