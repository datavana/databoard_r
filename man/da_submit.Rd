% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{da_submit}
\alias{da_submit}
\title{Submit tasks to the Databoard service}
\usage{
da_submit(data, col, task, options, wait = 0)
}
\arguments{
\item{data}{A data frame containing the input data.}

\item{col}{A column in \code{data} holding the text input. Tidy-evaluation is
supported (i.e. pass the bare column name).}

\item{task}{Character. The task the LLM is supposed to perform (e.g.
\code{"summarize"}, \code{"coding"}, \code{"annotate"}, \code{"triples"}).}

\item{options}{A named list of task-specific options passed to the API
(e.g. coding rules, model parameters).}

\item{wait}{Integer. Seconds to wait server-side for immediate completion of
each task before returning. \code{0} (default) returns immediately with a
\code{PENDING} state; larger values reduce the need for later \code{da_fetch()}
calls.}
}
\value{
The input data frame with additional columns:
\code{.task_id}, \code{.task_state}, and one column per field returned by the task
(unnested from \code{.task_result}).
}
\description{
Submits one Databoard task per row of \code{data}, using the values of \code{col} as
input. Task metadata (id, state) and results are stored back into \code{data} in
the special columns \code{.task_id}, \code{.task_state}, and \code{.task_result}. After
submission, results are unnested into regular columns and a progress
summary is printed.
}
\details{
Tasks are submitted sequentially, one per row. The function stops
immediately if a submission fails.
}
