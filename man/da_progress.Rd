% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{da_progress}
\alias{da_progress}
\title{Summarise task states of a Databoard data frame}
\usage{
da_progress(data, message = FALSE)
}
\arguments{
\item{data}{A data frame containing a \code{.task_state} column (as produced by
\code{\link[=da_submit]{da_submit()}} / \code{\link[=da_fetch]{da_fetch()}}).}

\item{message}{Logical. If \code{TRUE}, prints a nicely formatted, colourised
one-line summary via \code{\link[cli:cli_abort]{cli::cli_inform()}} and returns the counts
invisibly. If \code{FALSE} (default), just returns the counts.}
}
\value{
A named list with \code{TOTAL} (the total number of rows) and one entry
per observed task state.
}
\description{
Returns (and optionally prints) a summary of how many tasks are in each
state (e.g. \code{PENDING}, \code{SUCCESS}, \code{FAILURE}). The formatted message shows
the percentage per state on a single line, with colour coding.
}
