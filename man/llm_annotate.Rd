% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{llm_annotate}
\alias{llm_annotate}
\title{Automated annotation with an LLM}
\usage{
llm_annotate(data, col, rules, options = list(), wait = 0)
}
\arguments{
\item{data}{A data frame containing the texts to be annotated, or a data
frame previously returned by \code{llm_annotate()} whose pending results should be
fetched.}

\item{col}{A column in \code{data} holding the input text. Tidy-evaluation is
supported (pass the bare column name).}

\item{rules}{Required in submit mode. A data frame that is converted to an
array of dicts with keys \code{category}, \code{description}, and \code{example}.
The \code{description} is used to identify text segments. The \code{example} should
contain comma-separated text segments that match the rule. The \code{category}
is used as the \code{value} attribute in the annotation output.}

\item{options}{A named list of additional options passed to the Databoard
server. See the databoad server documentation for available options.}

\item{wait}{Integer. Seconds to wait server-side per case for the task to
complete before returning.
\itemize{
\item \code{0} (default): submit all tasks and return immediately with state
\code{PENDING}. Fetch results later by calling \code{llm_annotate(data)} again.
\item \verb{> 0}: wait up to that many seconds per case for the result.
}}
}
\value{
The input data frame with the columns \code{.task_id} and \code{.task_state}
added. When results are available, they are unnested into additional
result columns (e.g. \code{llm_result}). For annotation tasks, \code{llm_annos}
is added as a list-column with one data frame per case containing
\code{value} and \code{segment}.
}
\description{
Submits each row of \code{data} to the Databoard \code{annotate} task, which asks an
LLM to annotate the text in \code{col} using \code{rules}. The server post-processes
the answer and returns tabular output columns.
}
\details{
The function has two modes of operation, dispatched automatically:
\itemize{
\item \strong{Submit} - if \code{data} does \emph{not} yet contain a \code{.task_id} column, the
texts in \code{col} are submitted as new annotation tasks (via \code{\link[=da_submit]{da_submit()}}).
\item \strong{Fetch} - if \code{data} already contains a \code{.task_id} column (i.e. it was
previously returned by \code{llm_annotate()}), the function fetches results for
any tasks that are still pending (via \code{\link[=da_fetch]{da_fetch()}}). In this case,
all other parameters are ignored.
}

To customize prompts, pass \code{options$prompts$system} and/or
\code{options$prompts$user}.
}
\examples{
\dontrun{
anno_rules <- tibble::tribble(
  ~category, ~description, ~example,
  "PERSON", "Names of people", "John Doe, Jane Roe",
  "PLACE", "Names of places", "Berlin, New York"
)

# Submit
results <- llm_annotate(movies, abstract, anno_rules)

# Fetch pending tasks
results <- llm_annotate(results)

# Inspect annotations
head(results$llm_result)
results$llm_annos[[1]]
}

}
\seealso{
\code{\link[=llm_prompt]{llm_prompt()}}, \code{\link[=llm_code]{llm_code()}}, \code{\link[=llm_summarize]{llm_summarize()}}, \code{\link[=da_submit]{da_submit()}}, \code{\link[=da_fetch]{da_fetch()}}
}
