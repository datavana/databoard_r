% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{da_unnest}
\alias{da_unnest}
\title{Unnest the \code{.task_result} list-column into regular columns}
\usage{
da_unnest(data)
}
\arguments{
\item{data}{A data frame containing a \code{.task_result} list-column. If the
column is absent, \code{data} is returned unchanged.}
}
\value{
The data frame with \code{.task_result} unnested into individual columns.
}
\description{
Expands the tibble-valued \code{.task_result} column into one column per result
field. If the resulting columns already exist in \code{data}, only rows that
actually carry a task result (i.e. where \code{.task_result} is not \code{NA}) are
updated; other rows keep their existing values. The \code{.task_id} and
\code{.task_state} columns are moved to the end.
}
\keyword{internal}
