% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tasks.R
\name{llm_prompt}
\alias{llm_prompt}
\title{Prompt an LLM with custom prompts}
\usage{
llm_prompt(
  data,
  col,
  rules = NULL,
  prompt.system = NULL,
  prompt.user = NULL,
  options = list(),
  wait = 0
)
}
\arguments{
\item{data}{A data frame containing the texts to be processed, or a data
frame previously returned by \code{llm_prompt()} whose pending results should
be fetched.}

\item{col}{A column in \code{data} holding the input text. Tidy-evaluation is
supported (pass the bare column name).}

\item{rules}{Optional. A data frame with the columns \code{category},
\code{description}, and \code{example} (one row per category). If provided, a rule
book is generated from it and made available via the \code{{{rules}}}
placeholder in the prompts.}

\item{prompt.system}{The system prompt. Character vector; multiple elements
are collapsed with a line break. May contain the \code{{{text}}} and
\code{{{rules}}} placeholders.}

\item{prompt.user}{The user prompt. Character vector; multiple elements are
collapsed with a line break. May contain the \code{{{text}}} and \code{{{rules}}}
placeholders. In most cases, this is where you want to place \code{{{text}}}.}

\item{options}{A named list of additional options passed to the Databoard
server. See the Databoard server documentation for available options.}

\item{wait}{Integer. Seconds to wait server-side per case for the task to
complete before returning.
\itemize{
\item \code{0} (default): submit all tasks and return immediately with state
\code{PENDING}. Fetch results later by calling \code{llm_prompt(data)} again.
\item \verb{> 0}: wait up to that many seconds per case for the result.
}}
}
\value{
The input data frame with the columns \code{.task_id} and \code{.task_state}
added. When results are available, the raw LLM answer for each case is
returned in the \code{llm_result} column (as a character string; no parsing or
splitting is performed).
}
\description{
Sends a custom prompt to the LLM for each row of \code{data} and returns the raw
model output. Unlike \code{\link[=llm_code]{llm_code()}} and \code{\link[=llm_summarize]{llm_summarize()}}, which apply
task-specific pre- and post-processing on the Databoard server,
\code{llm_prompt()} bypasses all post-processing and hands back the answer as-is
in a single \code{llm_result} column. Use this function when you want full
control over the prompts and the output format.
}
\details{
Internally, \code{llm_prompt()} uses the Databoard \code{summarize} workflow but
overrides its prompts with the ones you provide.
}
\section{Placeholders}{
The prompts may contain the following placeholders, which are filled in
per case before the prompt is sent to the LLM:
\itemize{
\item \code{{{text}}} — replaced by the value of \code{col} for the current row.
\item \code{{{rules}}} — replaced by a rule book generated from the \code{rules} data
frame (only useful if \code{rules} is provided).
}
}

\section{Submit and fetch}{
Like the other \verb{llm_*()} wrappers, \code{llm_prompt()} has two modes of
operation, dispatched automatically:
\itemize{
\item \strong{Submit} — if \code{data} does \emph{not} yet contain a \code{.task_id} column, the
texts in \code{col} are submitted as new tasks (via \code{\link[=da_submit]{da_submit()}}).
\item \strong{Fetch} — if \code{data} already contains a \code{.task_id} column (i.e. it was
previously returned by \code{llm_prompt()}), the function fetches results for
any tasks that are still pending (via \code{\link[=da_fetch]{da_fetch()}}). In this case, all
other parameters are ignored.
}

Typical usage is to call \code{llm_prompt()} once to submit, and then call it
repeatedly on the returned data frame until all results are in (see
\code{\link[=da_finished]{da_finished()}}).
}

\examples{
\dontrun{
da_login()

# Submit and wait up to 10 seconds per case
data <- llm_prompt(
  songs, text,
  prompt.system = "Output a comma separated list of topics. Just the list, nothing else.",
  prompt.user = "{{text}}",
  wait = 10
)

# If any tasks are still pending, fetch them later
data <- llm_prompt(data)

# Inspect the raw answers
head(data$llm_result)
}

}
\seealso{
\code{\link[=llm_summarize]{llm_summarize()}}, \code{\link[=llm_code]{llm_code()}}, \code{\link[=da_submit]{da_submit()}}, \code{\link[=da_fetch]{da_fetch()}},
\code{\link[=da_finished]{da_finished()}}
}
