% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{tasks_run_post}
\alias{tasks_run_post}
\title{Submit a single task to the Databoard API (low level)}
\usage{
tasks_run_post(task, input, options, wait = 0)
}
\arguments{
\item{task}{Character. The task type (e.g. \code{"summarize"}, \code{"coding"},
\code{"annotate"}, \code{"triples"}).}

\item{input}{Character. The input text for the task. Must be non-empty.}

\item{options}{A named list of task-specific options passed to the API.}

\item{wait}{Integer. Seconds to wait server-side for the task to complete
before returning. Defaults to \code{0}.}
}
\value{
The server response
}
\description{
Sends one \code{POST /tasks/run} request to the Databoard API. This is the
underlying request helper used by \code{\link[=da_submit]{da_submit()}}; end users typically don't
need to call it directly.
}
\details{
Requires prior authentication via \code{\link[=da_login]{da_login()}}; the access token
is read from the \code{DATABOARD_ACCESSTOKEN} environment variable. Stops with
an informative error if the token is missing or the HTTP status is not
2xx.
}
\keyword{internal}
