---
title: "gettingstarted"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r}
library(tidyverse)
devtools::load_all(".")

test <- iris |> slice_sample(n=15)

rules <- list(
    contain_v = list(
        category    = "contain_v",
        description = "Code when versicolor is mentioned",
        example     = "versicolor"
    ),
    contain_s = list(
        category    = "contain_s",
        description = "Code when setosa is mentioned",
        example     = "setosa"
    )
)

#db_login("username", "password")
db_test <- .submit_task(test, Species, task = "coding", rules = rules)
db_test <- .get_taskresult(db_test)

test3 <- llm_code(test, "Species", rules  = rules)
test4 <- llm_summarize(test, "Species")

```

